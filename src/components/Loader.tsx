const Loader = () => {
  return (
    <div
      style={{
        color: "white",
        padding: "10px",
        margin: "0 auto",
        fontWeight: "bold",
        fontSize: '2rem',
        backgroundColor: "black",
      }}
    >
      Loading...
    </div>
  );
};

export default Loader;
